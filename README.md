run `bin/ast-project` on the file you want
look into bin/builtInInclude.path to find include path

run `bin/ast-project *file to run* -- -IPATH `

to run the comparison, execute
cd comp
./ifTransform.sh
